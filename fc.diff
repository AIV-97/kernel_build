--- arch/arm64/boot/dts/vendor/qcom/pmic632.dtsi
+++ arch/arm64/boot/dts/vendor/qcom/pmic632.dtsi
@@ -34,6 +34,18 @@
 	interrupts = <0 103 0>;
 	#address-cells = <1>;
 	#size-cells = <0>;
 
 	pmic-voter {
 		compatible = "qcom,pmic-voter";
 	};
 
+	/* Disable JEITA (Japan Electronics and Information Technology Industries Association) */
+	qcom,disable-jeita = <1>;
+	/* Bypass JEITA temperature regulation for charging */
+	qcom,jeita-bypass = <1>;
+	
+	/* Enable charging above JEITA recommended limits */
+	charge_limit {
+		qcom,jeita-override = <1>;
+	};
+	
 	pmi632_charger: qcom,qpnp-smb5 {
 		compatible = "qcom,qpnp-smb5";
 		reg = <0x68>;
@@ -40,6 +52,7 @@
 		interrupts = <0 58 0>;
 		...
 	};
 
+	/* Enable charging above JEITA recommended limits for battery */
+	charge_limit {
+		qcom,jeita-override = <1>;
+	};
 };

--- drivers/power/supply/pmic-voter.c
+++ drivers/power/supply/pmic-voter.c
@@ -36,6 +36,10 @@
 static int pmic_voter_notifier(struct notifier_block *nb, unsigned long event,
 				void *data)
 {
+	/* Override JEITA restrictions */
+	if (event == PMIC_VOTER_EVENT_CHARGE_LIMIT) {
+		/* Allow fast charge override */
+	}
 	return NOTIFY_OK;
 }
